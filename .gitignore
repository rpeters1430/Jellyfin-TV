*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing - never commit the keystore, its passwords, or the CI-only base64 encoding
# of it. These stay local; the actual secret values live in GitHub Actions repo secrets.
/keystore/
/app/keystore.properties
